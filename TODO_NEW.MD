# TODO

- [x] **Expand Candidate Window Range** - Increase the candidate chunk search window from current -2/+3 range to a wider range for better slide prediction accuracy. **Files:** `src/moves_cli/config.py` (update CANDIDATE_RANGE_MIN_OFFSET and CANDIDATE_RANGE_MAX_OFFSET)

- [x] **Tie-Breaking for Equal Similarity Scores** - When multiple candidates have equal similarity scores, implement tie-breaking logic to prefer the candidate whose target slide is closest to the current section on the right side (forward direction). This ensures natural forward progression during presentations. **Files:** `src/moves_cli/core/components/similarity_calculator.py` (add proximity-based sorting in compare method when scores are equal)

  > **Implemented (2024-12-27):** Added `current_section_index` parameter to `compare()` method. Sort now uses proximity-based tie-breaking: right-side (forward) candidates have priority 0, left-side have priority 1, then sorted by distance. Also removed the `raw_score >= 0.995` rounding to 1.0 (was display-only) to preserve score precision. This guarantees correct slide selection even with identical scores - critical for live presentations.

- [ ] **Strict Source File Validation** - Remove backup fallback system entirely and enforce source file presence for all operations. Implement xxhash-based change detection to alert users when source files have been modified since last processing. During processing, copy source to temp directory to prevent file locking issues. Before control command, verify source exists (error if missing) and check hash (warning with confirmation prompt if changed). This prevents silent failures with stale data during live presentations. **Files:** `src/moves_cli/models.py` (add hash fields to Speaker), `src/moves_cli/core/speaker_manager.py` (remove backup logic, compute hashes, temp copy), `src/moves_cli/cli.py` (implement validation and prompts in control command)

- [ ] **Presentation Control Terminal UI** - Develop rich-based terminal interface with integrated state machine for presentation control. Interface should include global keyboard listener (M key for PAUSE/ACTIVE toggle, arrow keys for manual intervention detection with echo prevention), sync lock mechanism (automatic LOCKED state on external arrow press, consensus-based unlock when top result matches current section), and visual state indicators (ACTIVE/PAUSED/LOCKED with color coding). Display real-time slide position, similarity scores, speech preview, and match preview in organized layout. State machine should handle transitions between ACTIVE (auto-control), PAUSED (user control), and LOCKED (waiting for consensus) states. **Files:** `src/moves_cli/core/presentation_controller.py`

- [ ] **CLI Output Unification** - Replace all scattered typer.echo calls throughout the codebase with centralized output utility using rich library for consistent formatting. **Files:** All files currently using typer.echo

- [ ] **Enrich CLI Output Messages** - Improve CLI output messages to be more informative and user-friendly. Display critical file paths (sections.json location, speaker data directory, settings.toml path) in relevant commands. Restructure sentence patterns to be clearer and more descriptive. Add contextual information such as "Speaker data stored at ~/.moves/speakers/speaker-id" after operations, "Sections file: ~/.moves/speakers/speaker-id/sections.json" when checking status, and "Configuration file: ~/.moves/settings.toml" in settings commands. Use consistent terminology and formatting across all commands. **Files:** `src/moves_cli/cli.py`, `src/moves_cli/core/speaker_manager.py`

- [ ] **Migrate sections.json to YAML** - Replace JSON format with YAML using folded block scalars for better human readability and manual editing. YAML format should use folded block scalar (>) for content fields, maintain section_index first for readability, and wrap lines at 80 characters (76 content + 4 indent). Implement auto-detection to support both JSON and YAML formats based on file extension while maintaining backward compatibility with existing JSON files. **Files:** `src/moves_cli/core/components/section_producer.py`, `src/moves_cli/core/speaker_manager.py` **Reference:** `experiments/yaml_tests/`

- [ ] **Documentation Rewrite** - Rewrite entire documentation from scratch to reflect current architecture and features. Documentation should cover: Architecture overview with component interactions and data flow, installation and setup guide for different platforms, CLI command reference with examples for all commands, configuration options and settings management, speaker processing workflow from PDF input to live control, presentation control usage and supervisor protocol (normal flow, planned intervention with M key, emergency intervention with direct arrow press and consensus-based unlock), data models and file formats (sections, chunks, embeddings), troubleshooting common issues and error messages, and development guide for contributors. Remove outdated information and ensure all examples are tested and working. Use consistent terminology throughout. **Files:** Entire `docs/` directory

- [ ] **Test Suite Implementation** - Write comprehensive test suite from scratch covering all core functionality. Tests should include: Unit tests for text normalization and similarity calculation, integration tests for section producer with mock LLM responses, integration tests for chunk producer and candidate generation, tests for speaker manager operations (add, edit, delete, process), tests for presentation controller state management, tests for CLI commands and output formatting, edge cases (empty inputs, malformed data, missing files), and performance tests for real-time audio processing pipeline. Use pytest with appropriate fixtures and mocking. Aim for high code coverage on critical paths. **Files:** `tests/` directory

- [ ] **Refactor Duplicate Path/Status Logic** - Add `data_dir` and `sections_file` properties to Speaker model to eliminate duplicate path construction logic scattered across CLI commands. Add `is_ready` property to check if speaker has been processed. This centralizes path management and makes code more maintainable. **Files:** `src/moves_cli/models.py` (add properties to Speaker class), `src/moves_cli/cli.py` (use speaker.sections_file instead of manual path construction)

- [ ] **Add Speaker Path Properties and Sections Filename Constant** - Extract SECTIONS_FILENAME constant to config.py for centralized control (preparation for YAML migration). Add Speaker.data_dir and Speaker.sections_file properties for encapsulated path management. Remove hardcoded "sections.json" strings throughout codebase. **Files:** `src/moves_cli/config.py` (add SECTIONS_FILENAME constant), `src/moves_cli/models.py` (add data_dir and sections_file properties), `src/moves_cli/cli.py`, `src/moves_cli/core/speaker_manager.py`, `src/moves_cli/core/presentation_controller.py` (replace hardcoded strings with constant/properties)

- [ ] **Create Datetime Formatting Utility** - Extract duplicate datetime parsing and formatting logic into reusable utility function. Currently the same try-except pattern for parsing ISO datetime strings and formatting them is repeated in multiple CLI commands. **Files:** `src/moves_cli/utils/formatters.py` (create format_datetime function), `src/moves_cli/cli.py` (replace duplicate datetime formatting code in speaker_list and speaker_show commands)

- [x] **Add Magic Number Constants** - Replace magic numbers with named constants in config.py for better maintainability. Examples: SPEAKER_ID_GENERATION_MAX_RETRIES (currently hardcoded as 3), SPEAKER_ID_SUFFIX_LENGTH (currently 5). **Files:** `src/moves_cli/config.py` (add constants), `src/moves_cli/core/speaker_manager.py` (use constants instead of magic numbers)

- [x] **Optimize Speaker Resolve Logic** - Replace O(n) list operations with O(1) dictionary lookup in speaker resolution. Currently creates speaker_ids list and uses index() which is inefficient. Use dictionary mapping for direct access. **Files:** `src/moves_cli/core/speaker_manager.py` (refactor resolve method to use dict-based lookup)

- [x] **Extract File Resolution Helper** - Simplify backup file resolution logic by extracting 24-line source/backup file checking code into reusable helper function. Currently duplicated for presentation and transcript files. **Files:** `src/moves_cli/core/speaker_manager.py` (create \_resolve_file helper method, use it in process method)

- [x] **Remove Commented Code** - Clean up commented-out code blocks that add noise without value. Examples include commented typer.echo statements with explanatory comments. Either remove entirely or convert to proper TODO comments if functionality is planned. **Files:** `src/moves_cli/cli.py`, `src/moves_cli/core/speaker_manager.py`

- [x] **Add Missing Type Hints** - Add type hints to functions missing them, particularly signal handlers and internal helper functions. Improves type safety and IDE support. **Files:** `src/moves_cli/core/speaker_manager.py` (add type hints to sigint_handler and other internal functions)
